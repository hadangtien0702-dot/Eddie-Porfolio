# 02 — Update 2026-03-18

> **Ngày**: 2026-03-18
> **Loại**: UI/UX Improvements + Bug Fixes + New Features
> **Trạng thái**: Hoàn thành

---

## 1. Navigation — Active State + Glow Border + Scroll Fix

**File**: `src/components/00-Navigation/Navigation.tsx`

### Thay đổi
- [x] **Active section tracking** — thêm `IntersectionObserver` theo dõi section đang trong viewport, nav item tương ứng sáng lên
- [x] **Active dot indicator** — chấm đỏ xuất hiện dưới item active, Framer Motion `layoutId` animate mượt khi chuyển section
- [x] **Glow border line** — đường sáng đỏ chạy quanh viền nav khi scroll, dừng giữ nguyên vị trí khi không scroll (RAF-based, `useMotionValue`)
- [x] **Nav luôn hiển thị** — bỏ hide-on-scroll, nav cố định trên màn hình
- [x] **Smooth scroll fix** — thêm `onClick` → `scrollIntoView({ behavior: 'smooth' })` thay vì dùng anchor link thuần (Next.js intercept)
- [x] **Mobile menu** — smooth scroll + đóng menu sau khi click

---

## 2. Overview — Shader Line dưới Hero Image

**File**: `src/components/02-Overview/Overview.tsx`

### Thay đổi
- [x] **Animated shader line** — tia sáng gradient đỏ-trắng-đỏ quét từ trái → phải, loop infinitely (`repeatDelay: 0.8s`)
- [x] **Glow ellipse** — pulse đồng bộ với tia sáng, tạo chiều sâu bên dưới hero image

---

## 3. Work Section — Rename + VideoCarousel3D Redesign

**Files**: `src/components/04-Work/Work.tsx`, `src/components/04-Work/VideoCarousel3D.tsx`

### Rename
- [x] `04-Services/Services.tsx` → `04-Work/Work.tsx`
- [x] Export function `Services()` → `Work()`
- [x] `src/app/page.tsx` import updated

### VideoCarousel3D — Viết lại hoàn toàn
**Vấn đề cũ:**
- Drag div `z-40` che hết card → không click được
- Auto-rotate dùng `document.activeElement` → unreliable
- Ring 360° với 24 items → lặp lại, khó chọn

**Giải pháp mới:**
- [x] **Coverflow 3D** — 6 cards với spring animation, center card nổi bật (scale 1.0), side cards mờ dần (rotateY ±42°)
- [x] **Click trực tiếp** — pan surface `z-0` phía sau, card `z-10` phía trên → click từng card được
- [x] **Auto-rotate** — `setInterval` 4s, reset khi user tương tác
- [x] **Keyboard nav** — `ArrowLeft/Right` + `Escape`
- [x] **Swipe/drag** — `onPanEnd` detect offset > 60px
- [x] **Detail panel** — hiện dưới carousel: description, client, views, tags, "Watch Full Video" CTA
- [x] **Dot navigation** — 6 dots, active dot expand thành pill đỏ
- [x] **Accent glow border** — center card có `box-shadow` accent khi selected

---

## 4. Work Data — Fix ảnh bị lỗi

**File**: `src/data/services.ts`

### Thay đổi
- [x] Fix 4 image paths trỏ tới file không tồn tại
- [x] Map lại với file thực tế trong `/public/images/services/`:
  - Setup and Build → `content-growth.jpg`
  - Web & Forum Design → `motion-graphics.jpg`
  - Social → `brand-content.jpg`
  - Video Editor → `video-strategy.jpg`
  - A.I → `performance-ads.jpg`

> **Lưu ý**: Cần thay bằng ảnh thực tế của project khi có. Đặt vào `/public/images/services/` và update path trong `services.ts`.

---

## Tổng kết

| Section | Trạng thái |
|---------|-----------|
| Navigation | ✅ Hoàn chỉnh |
| Overview shader line | ✅ Hoàn chỉnh |
| Work rename | ✅ Hoàn chỉnh |
| VideoCarousel3D redesign | ✅ Hoàn chỉnh |
| Work images fix | ✅ Hoàn chỉnh |
